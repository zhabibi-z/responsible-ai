# Compatible with Python 3.14+
numpy>=2.4
pandas>=3.0
# Pinned: the committed model/preprocessor pickles were trained on 1.9.0;
# loading them under a different scikit-learn can break or warn. Keep this in
# sync with hf_space/requirements.txt.
scikit-learn==1.9.0
xgboost>=2.1
shap>=0.46
lime>=0.2.0
fairlearn>=0.11
matplotlib>=3.9
seaborn>=0.13
kaggle>=1.6

# Imported by the notebook but not pinned in the install cell.
imbalanced-learn>=0.12
joblib>=1.4

# Listed in the install cell for planned monitoring/app sections.
evidently>=0.4
gradio>=4.20

# Dev / evaluation tooling.
pytest>=8.0

# Note: google.colab (userdata) is used only for the optional Kaggle data
# path and is provided by the Colab runtime, so it is not listed here.
